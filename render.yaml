services:
- type: web
  name: shinhanlife-agent-api
  env: node
  plan: free
  buildCommand: "npm install"
  startCommand: "npm start"
  envVars:
  - key: FRONTEND_ORIGIN
    value: https://YOUR-NETLIFY-SITE.netlify.app
  autoDeploy: true
  healthCheckPath: /api/health
